services:
  - type: web
    name: foldfresh-app
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd frontend && npm install && npm run build
    startCommand: gunicorn foldfresh_django.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: foldfresh_django.settings
